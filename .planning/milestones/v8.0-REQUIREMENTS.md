# Milestone 8 Requirements (Archived)

## Requisitos Entregues

### 1. Destaque Visual no JobCard (`JobCard.tsx`)
- [x] **REQ-801**: Localizar o parágrafo da justificativa da IA (`job.aiReasoning`).
- [x] **REQ-802**: Destacar a palavra **"Aprovada"** em verde (`text-emerald-400 font-semibold`).
- [x] **REQ-803**: Destacar as palavras **"Rejeitada"** ou **"Descartada"** em vermelho (`text-red-400 font-semibold`).

### 2. Heurística Hub Florianópolis & Filtro de Cidade
- [x] **REQ-804**: Atualizar `HermesEvaluator` (`evaluate` e `evaluateHeuristic`) para aceitar vagas **Presencial** ou **Híbrido** em **Florianópolis / Floripa / Florianópolis - SC** (`locationScore = 100`).
- [x] **REQ-805**: Presencial ou Híbrido em outras cidades fora da Grande Florianópolis (São Paulo, Curitiba, BH, etc.) continuam com `locationScore = 0` (Rejeitadas).
- [x] **REQ-806**: Adicionar campo/filtro de **Cidade / Localização** no Explorador de Vagas (`/jobs`), com suporte a busca textual por localização e dropdowns de cidades-chave (Todas, Remoto, Florianópolis, São José, Palhoça, etc.).
- [x] **REQ-807**: Adicionar suporte a parâmetro `location` na API `GET /api/jobs` e `JobRepository`.

### 3. Conector 99Freelas com Trava de 48 Horas (`99freelas.ts`)
- [x] **REQ-808**: Criar adaptador `99FreelasScraper` para `https://www.99freelas.com.br/projects`.
- [x] **REQ-809**: Filtrar na ingestão apenas projetos compatíveis com a stack de Moacir Neto (React, Next, Node, JS, TS, Python, IoT, etc.).
- [x] **REQ-810**: **Trava de 48h**: Se a publicação for superior a 2 dias (48 horas), descartar o projeto na ingestão.
- [x] **REQ-811**: Atribuir a categoria `Freelance` para essas oportunidades.

### 4. Expansão de 10 Novas Fontes de Vagas Tech
- [x] **REQ-812**: Criar e registrar adaptadores para os 10 sites:
  1. `GeekHunterScraper` (`geekhunter.com/pt/vagas`)
  2. `NerdinScraper` (`nerdin.com.br`)
  3. `ProgramathorScraper` (`programathor.com.br`)
  4. `ReveloScraper` (`app.careers.revelo.com/home`)
  5. `NoventaENoveJobsScraper` (`99jobs.com`)
  6. `SolidesScraper` (`vagas.solides.com.br`)
  7. `RunTalentScraper` (`runtalent.zohorecruit.com/jobs/Careers`)
  8. `EmpregareScraper` (`empregare.com/pt-br/vagas`)
  9. `WorkanaScraper` (`workana.com/pt/jobs?language=pt`)
  10. `TramposScraper` (`trampos.co/oportunidades`)
- [x] **REQ-813**: Utilizar `createStealthContext` (Playwright) ou headers customizados para contornar proteções anti-bot / Cloudflare com resiliência total.
- [x] **REQ-814**: Atualizar enum `PlatformSource`, mapeamentos de cores e `ScraperOrchestrator` para rodar todas as fontes integradas (35+ fontes ativas).
